{
  "name": "dronecontrol",
  "version": "1.0.0",
  "description": "Simple server to interpret data recived by the Red5 app and convert it to drone commands",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "NotHawk",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.5.7",
    "express": "^4.16.2"
  }
}
